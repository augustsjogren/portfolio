name: Pull Request

on:
  pull_request:

jobs:
  verify:
    name: Build Pull Request
    uses: './.github/workflows/build.yml'
    secrets: inherit

  deploy:
    name: Deploy site
    uses: './.github/workflows/deploy.yml'
