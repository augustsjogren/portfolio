import React from 'react';

import profileImage from '../images/Aggebagge_web.webp';

import '../styles/image.scss';

const BioImage = () => {
  return <img alt='' src={profileImage} className='bio-img' />;
};

export default BioImage;
